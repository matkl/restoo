{% extends "RestooMainBundle::layout.html.twig" %}

{% block title "Jobs" %}
{% block subheadline "Jobs" %}

{% block submenu %}
	{% include 'RestooMainBundle:Job:submenu.html.twig' %}
{% endblock %}

{% block content %}
	<table>
	<tr>
		<th>Alias</th>
		<th>Title</th>
		<th>Created by</th>
		<th>Assigned to</th>
	</tr>
	{% for job in jobs %}
		<tr>
			<td><a href="{{ path( 'job_show', { 'id': job.id } ) }}">{{ job.alias }}</td>
			<td>{{ job.title }}</td>
			<td>{{ job.reporter }}</td>
			<td>{{ job.receiver }}</td>
		</tr>
	{% else %}	
		<tr>
			<td colspan="3">No jobs found</td>
		</tr>
	{% endfor %}
	
	</table>

{% endblock %}