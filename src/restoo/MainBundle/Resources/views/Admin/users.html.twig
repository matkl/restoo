{% extends "RestooMainBundle::layout.html.twig" %}

{% block title "Administration: User" %}
{% block subheadline "Administration: User" %}

{% block submenu %}
	{% include 'RestooMainBundle:Admin:submenuUser.html.twig' %}
{% endblock %}

{% block content %}
	<table>
		<tr>
			<th>Username</th>
			<th>Name</th>
			<th>E-Mail</th>
			<th>Team</th>
			<th>Roles</th>
			<th>Actions</th>
		</tr>
	{% for user in users %}
		<tr>
			<td>{{ user.username }}</td>
			<td>{{ user.lastname }}, {{ user.firstname }}</td>
			<td>{{ user.email }}</td>
			<td>{{ user.team }}</td>
			<td>
				{% for role in user.roles %}
					{{ role }}<br />
				{% endfor %}
			</td>
			<td>
				<a href="{{ path( 'admin_user_edit', { 'id': user.id } ) }}">edit</a> 
				<a href="{{ path( 'admin_user_delete', { 'id': user.id } ) }}">delete</a> 
			</td>
		</tr>
	{% endfor %}
	</table>
{% endblock %}

{% block sidebar %}
	###ADD FILTER###
{% endblock %}	